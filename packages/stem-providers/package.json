{
  "name": "@song-maker/stem-providers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 3 — StemSeparatorProvider interface, HTDemucs adapter, BS-RoFormer stub",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
