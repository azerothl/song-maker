{
  "name": "song-maker",
  "private": true,
  "version": "0.0.0",
  "description": "Song Maker monorepo — phases 3–4 foundations (stems, mix, LoRA, worker, agent)",
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
